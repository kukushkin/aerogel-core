en:
    aerogel:
        db:
            errors:
                failed_to_save:
                    "Failed to save %{name}: %{errors}"

    errors:
        messages:
            invalid_format: has invalid format
            unique: must be unique
